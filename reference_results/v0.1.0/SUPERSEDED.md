# Superseded Reference Result

`v0.1.0` is preserved for historical comparison.

It has been superseded by `reference_results/v0.1.1/`, which uses the same
mathematical model and inputs but records frozen decision thresholds,
`protocol_sha256`, and `derivation_version` in the protocolized audit chain.

Do not treat this directory as the current protocol truth.
